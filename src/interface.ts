export interface IWebShopItem {
    id: number,
    title: string,
    price: number,
    imageURL: string,
}

export interface IProject {
    name: string,
    description: string,
    URL: string,
    imageURL: string,
}